services:
  cloudflared:
    image: cloudflare/cloudflared:2025.10.0
    container_name: cloudflared
    env_file:
      - ~/config/cloudflared/app.env
    networks:
      - common_network
    command: tunnel --no-autoupdate run
    restart: always

networks:
  common_network:
    external: true
